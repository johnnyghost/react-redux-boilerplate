import React, { Component } from 'react';

class AuthenticationView extends Component {
  render() {
    return (
      <section className="authentication row">
      </section>
    );
  }
}

export default AuthenticationView;
