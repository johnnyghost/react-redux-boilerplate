import React from 'react';
import './assets/styles/header.scss';

const Header = () => {
  return (
    <header className="header">
        <div className="header__logo">ghost</div>
    </header>
  );
};

export default Header;
